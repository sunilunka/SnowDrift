events {}

http {
    server {
        listen 8666;
        location / {
            add_header 'Access-Control-Allow-Origin' '*' always;

            proxy_pass http://host.docker.internal:2501;
        }
    }
}